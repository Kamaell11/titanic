image: python:3.8

stages:
  - build
  - deploy

before_script:
  - apt-get update && apt-get -y install git

build:
  stage: build
  script:
    - pip install -r requirements.txt

deploy:
  stage: deploy
  script:
    - pip install streamlit 
    - streamlit run app.py --server.port $CI_SERVER_PORT
  rules:
    - exists:
        - app.py
  only:
    - master

